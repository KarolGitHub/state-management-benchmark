# Getting Started

```sh
yarn install
yarn start
```

Open [http://localhost:3000](http://localhost:3000).

Included apps:

- App #1 (ReactJS - acts as the app shell plus is an aggregation with other remotes): [http://localhost:3000](http://localhost:3000)
